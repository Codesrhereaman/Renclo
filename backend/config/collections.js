// Firestore collection name constants — single source of truth
const COLLECTIONS = {
  USERS: 'users',
  PRODUCTS: 'products',
  ORDERS: 'orders',
  CARTS: 'carts',
  WISHLISTS: 'wishlists',
  REVIEWS: 'reviews',
};

module.exports = COLLECTIONS;
